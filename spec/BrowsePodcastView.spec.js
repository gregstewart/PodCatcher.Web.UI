describe('BROWSE PODCAST VIEW', function () {
  beforeEach(function () {
  });

  it('should initialise the view with a collection', function () {
//    var collection = new Backbone.Collection();
//    var collectionStub = sinon.stub(collection, 'fetch').returns(null);
//    var view = new Podcatcher.BrowsePodcastView({collection: collection});
//
//    expect(collectionStub.calledOnce).toBe(true);
  });
});